# one time each project to start git
git init

# every time to add something
git add .

# every time you commit
git commit -m "first commit"

# one time each project when you are child developer . but when i will elder then it needed.
git branch -M main

#one time each project
git remote add origin https://github.com/abdullahalazmain/My-first-github-repo.git

# one time you push in origin main
git push -u origin main

# everytime 
git push